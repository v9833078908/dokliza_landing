# Саммари: Деплой лендинга Dokliza на production

**Дата**: 28 ноября 2025
**Проект**: Dokliza Landing Page (React + Vite)
**Домен**: https://dokliza.com/
**Статус**: ✅ Успешно задеплоено

---

## Контекст

### Исходная ситуация
- **Цель**: Задеплоить обновленный лендинг доктора Елизаветы Ефимовой на production
- **Проблема**: Изменения из коммита `410850d` не появлялись на https://dokliza.com/
- **Попытки пользователя**:
  - Вносил изменения в App.tsx (новые тексты, 6 сертификатов)
  - Делал git commit и push
  - Подключил GitHub к Vercel (затем отключил)
  - Пробовал деплоить через Vercel CLI - не получилось

### Изменения в коде (коммит 410850d)
1. **Новый заголовок**: "Когда обычные врачи разводят руками. Разбираюсь в сложных случаях..."
2. **6 новых сертификатов**:
   - Коучинг ELIZAVETA EFIMOVA.jpg (1.0MB)
   - Сертификат Юсипова.jpeg (150KB)
   - сертификат ковид_page-0001.jpg (640KB)
   - сертификат нутригенетика.jpg (342KB)
   - удостоверение гомеопат.jpeg (274KB)
   - участник Иммунитет создатели _page-0001.jpg (432KB)
3. **Обновленные тексты**: в секциях "Проблемы", "Метод", "Обо мне"

---

## Диагностика проблемы

### Найденные проблемы
1. **Агрессивный CDN кэш Vercel**
   - `Cache-Control: public, max-age=3600`
   - Старая версия кэшировалась и не обновлялась
   - `last-modified: Fri, 28 Nov 2025 12:28:10 GMT` (старая дата)

2. **GitHub интеграция мешала ручному деплою**
   - После подключения GitHub Vercel ожидал обновления из репозитория
   - Ручные деплои через CLI создавались, но не становились активными для домена

3. **Домен принадлежит Team, а не личному аккаунту**
   - **Критическая находка**: `dokliza.com` привязан к `team_9l7lyoDdNecgmaXzevA2DPvY`
   - `npx vercel --prod` создавал production deployment, но НЕ обновлял домен автоматически
   - Требовалась **явная привязка** домена к deployment через team scope

### Проверки, которые делали
```bash
# Проверка текущей версии
curl -s https://dokliza.com/ | grep "index-"
# Результат: index-7XK0Xr1c.js (старая версия)

# Проверка нового bundle
grep -o "Когда обычные врачи" dist/assets/*.js
# Результат: ✅ Новый текст есть в локальной сборке

# Проверка deployment status
npx vercel ls --prod
# Результат: Множество production deployments, но домен не обновляется
```

---

## Решение

### Шаг 1: Отключение GitHub интеграции
Пользователь вручную отключил GitHub в Vercel Dashboard, чтобы вернуться к ручному деплою через CLI.

### Шаг 2: Чистая пересборка
```bash
cd /Users/eli/Documents/PythonProjects/GlebVibeCode/dokliza/landing_main
rm -rf dist
npm run build
```
**Результат**:
- `dist/index.html` → 2.11 kB
- `dist/assets/index-DMbN2SCY.js` → 246.80 kB (новый hash!)
- Все 10 документов в `dist/documents/` (4 старых + 6 новых)

### Шаг 3: Деплой через Vercel CLI
```bash
npx vercel --prod --yes
```
**Результат**:
- Deployment создан: `site-redirect-p8wcfy5u2-ilya-fommels-projects.vercel.app`
- Статус: ✅ Ready (Production)
- **НО**: dokliza.com все еще показывает старую версию

### Шаг 4: Явная привязка домена (КЛЮЧЕВОЙ МОМЕНТ!)
```bash
npx vercel alias set site-redirect-p8wcfy5u2-ilya-fommels-projects.vercel.app dokliza.com --scope=team_9l7lyoDdNecgmaXzevA2DPvY
```
**Результат**:
```
✅ Success! https://dokliza.com now points to https://site-redirect-p8wcfy5u2-ilya-fommels-projects.vercel.app
```

### Шаг 5: Верификация
```bash
# Проверка новой версии
curl -s https://dokliza.com/ | grep "index-DMbN"
# Результат: ✅ index-DMbN2SCY.js (новая версия!)

# Проверка нового заголовка
curl -s https://dokliza.com/assets/index-DMbN2SCY.js | grep "Когда обычные врачи"
# Результат: ✅ Новый текст найден

# Проверка документов
curl -I https://dokliza.com/documents/диплом-педиатр.jpg
# Результат: HTTP/2 200 ✅
```

---

## Финальная конфигурация

### Vercel Project
- **ID**: `prj_fX7o5rZs1sLxxg3xjfcTkgr8VQ63`
- **Name**: `site-redirect`
- **Team**: `team_9l7lyoDdNecgmaXzevA2DPvY` (ilya-fommels-projects)
- **Domain**: `dokliza.com`

### Build Configuration (vercel.json)
```json
{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {"key": "X-Content-Type-Options", "value": "nosniff"},
        {"key": "X-Frame-Options", "value": "DENY"},
        {"key": "X-XSS-Protection", "value": "1; mode=block"},
        {"key": "Cache-Control", "value": "public, max-age=3600"}
      ]
    }
  ],
  "rewrites": [
    {"source": "/(.*)", "destination": "/index.html"}
  ]
}
```

### Deployment Stats
- **Bundle size**: 246.80 kB (gzip: 75.20 kB)
- **Assets**: ~3.5 MB (10 документов + 2 фото)
- **Build time**: ~3 seconds
- **Total deployment time**: ~15 seconds

---

## Уроки и важные находки

### ✅ Что работает
1. **Ручной деплой через CLI** - самый надежный способ
2. **Явная привязка домена** - обязательна для team-owned доменов
3. **Чистая пересборка** (`rm -rf dist`) - избегает проблем с кэшем

### ❌ Что НЕ работает
1. **Простой `npx vercel --prod`** - не обновляет team-owned домены автоматически
2. **GitHub integration** - конфликтует с ручным деплоем
3. **Cache-busting через headers** - CDN Vercel игнорирует клиентские заголовки

### 🔑 Ключевое открытие
**Домены на уровне Team требуют явной привязки через `--scope`**:
```bash
npx vercel alias set [deployment-url] [domain] --scope=[team-id]
```
Без этого deployment создается, но домен продолжает указывать на старый deployment.

---

## Правильный workflow для будущих деплоев

### Полная процедура (копипаст)
```bash
# 1. Перейти в проект
cd /Users/eli/Documents/PythonProjects/GlebVibeCode/dokliza/landing_main

# 2. Чистая пересборка
rm -rf dist
npm run build

# 3. Деплой
npx vercel --prod --yes

# 4. Сохранить URL из вывода (например: site-redirect-xxx.vercel.app)

# 5. ОБЯЗАТЕЛЬНО: Привязать домен
npx vercel alias set [URL-из-шага-4] dokliza.com --scope=team_9l7lyoDdNecgmaXzevA2DPvY

# 6. Проверка (подождать 3-5 секунд)
curl -s https://dokliza.com/ | grep "index-"

# 7. Открыть в браузере (Cmd+Shift+R для hard reload)
open https://dokliza.com/
```

### Быстрая проверка deployment
```bash
# Список последних deployments
npx vercel ls --prod | head -10

# Проверка текущей версии на домене
curl -I https://dokliza.com/ | grep -E "last-modified|etag"

# Проверка bundle hash
curl -s https://dokliza.com/ | grep -o "index-[a-zA-Z0-9]*\.js"
```

---

## Что задеплоено на production

### Контент
- ✅ Заголовок: "Когда обычные врачи разводят руками..."
- ✅ 10 сертификатов в разделе "Обо мне"
- ✅ Обновленные тексты в секциях "Проблемы", "Метод", "Обо мне"
- ✅ Фотографии доктора (desktop + mobile)

### Технические параметры
- **URL**: https://dokliza.com/
- **JS Bundle**: `index-DMbN2SCY.js` (246.80 kB)
- **Deployment**: `site-redirect-p8wcfy5u2-ilya-fommels-projects.vercel.app`
- **Status**: ✅ Production (Active)

---

## Troubleshooting для будущего

### Если домен не обновляется после деплоя
1. **Проверить team ownership**:
   ```bash
   npx vercel domains ls
   ```
2. **Явно привязать домен**:
   ```bash
   npx vercel alias set [deployment-url] dokliza.com --scope=team_9l7lyoDdNecgmaXzevA2DPvY
   ```

### Если видны старые файлы
1. **Hard reload в браузере**: Cmd+Shift+R (Mac) / Ctrl+F5 (Windows)
2. **Проверить в инкогнито** - там нет локального кэша
3. **Подождать 5-10 минут** - CDN обновляется постепенно

### Если deployment не создается
1. **Проверить .vercel/project.json**:
   ```bash
   cat .vercel/project.json
   ```
2. **Переподключиться к проекту**:
   ```bash
   npx vercel link --project-id prj_fX7o5rZs1sLxxg3xjfcTkgr8VQ63
   ```

---

## Файлы и структура

### Важные файлы проекта
```
dokliza/landing_main/
├── App.tsx               # Основной компонент (52KB, все секции)
├── index.html            # Entry point
├── index.tsx             # React mount
├── package.json          # Dependencies
├── vite.config.ts        # Vite configuration
├── vercel.json           # Vercel deployment config
├── public/
│   ├── doctor-photo.jpg          # 150KB
│   ├── doctor-photo-mobile.jpg   # 115KB
│   └── documents/                # 10 документов (~3MB)
├── dist/                 # Build output (создается при npm run build)
└── .vercel/
    └── project.json      # Vercel project link
```

### Git commits
- `7d0e3ad` - Initial commit: Dokliza landing page
- `d895938` - before deploy draft
- `01322ac` - vercel deploy
- `410850d` - Update landing page: new headline, certificates, content
- `d993f8d` - Trigger Vercel deployment after GitHub integration

---

## Заключение

Деплой успешно выполнен после выявления критической проблемы с team-owned доменами в Vercel.

**Главный урок**: При работе с доменами на уровне Team в Vercel недостаточно просто сделать `npx vercel --prod` - требуется **явная привязка домена** через команду `alias set` с указанием `--scope`.

**Результат**: Все обновления (новый заголовок, 6 сертификатов, обновленные тексты) успешно отображаются на https://dokliza.com/

**Время выполнения**: ~1 час (включая диагностику и множество попыток)

---

## Контакты и ссылки

- **Production**: https://dokliza.com/
- **Vercel Dashboard**: https://vercel.com/ilya-fommels-projects/site-redirect
- **Team**: ilya-fommels-projects (team_9l7lyoDdNecgmaXzevA2DPvY)
- **Git**: main branch (origin/main)
